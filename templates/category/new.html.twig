{% extends "base.html.twig" %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-body">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary">Ajoutez une catégorie</button>
                {{ form_end(form) }}
            </div>
        </div>
        <h2>
            <a href="{{ path('category_index') }}">
                Retour aux catégories
            </a>
        </h2>
    </div>


{% endblock %}
